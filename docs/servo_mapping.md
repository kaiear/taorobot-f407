# 舵机映射记录

## 底层接口

源码位置：

```text
firmware/SJRobot_center/Hardware_program/y_servo.c
```

核心接口：

```c
duoji_doing_set(index, aim, time);
```

## PWM 范围

源码限制：

```c
if (aim > 2490) aim = 2490;
else if (aim < 510) aim = 510;
```

因此舵机 PWM 范围大致为：

```text
510 ~ 2490 us
```

## 特殊反向

源码中 `index == 3` 时做了反向：

```c
if(index == 3)
{
    aim = 3000 - aim;
}
```

机械臂调试时必须记录每一路舵机的实际方向、零位和安全范围。

## 调试变量

Keil Debug / Watch 中使用以下变量逐个测试：

| 变量名 | 作用 | 建议初值 | 说明 |
|---|---|---:|---|
| `servo_test_enable` | 开启测试入口 | `1` | `0` 时不会发送测试命令 |
| `servo_test_id` | 当前测试舵机 ID | `0` | 每次只测试一个 ID |
| `servo_test_pos` | 目标 PWM 位置 | `1500` | Debug 测试限幅为 `600~2400`，改完位置后还要触发 `send` |
| `servo_test_time` | 动作时间 ms | `1500` | 首次测试建议慢一点 |
| `servo_test_send` | 单次发送触发 | `1` | 每发送一次会自动清回 `0` |

## 舵机 ID 对照表

先确认“哪个物理舵机对应哪个 ID”，不要急着装连杆。

| 测试顺序 | 物理舵机/关节 | 舵机 ID | `1500` 状态 | `1450` 变化 | `1550` 变化 | 是否正常 | 备注 |
|---:|---|---:|---|---|---|---|---|
| 1 | 底座旋转 |  0| 朝向正前方 | 顺 |逆  |正常  |  |
| 2 | 大臂/肩关节 |  1| 实际初始化不在15001500时微微低头 | 向前 | 向后 | 正常 |  |
| 3 | 小臂/肘关节 |  2|打直  |仰  |俯  |  正常|  |
| 4 | 腕部俯仰 | 3 | 打直 |  俯|仰  |正常  |  |
| 5 | 夹爪旋转 |4  | 中位 | 朝小车正向看逆时针 | 顺时针 |正常  |  |
| 6 | 夹爪开合 |  5|  连杆夹角70度| 夹角变大 | 变小 | 正常 |  |
| 7 | 备用/未知 |  |  |  |  |  |  |
| 8 | 备用/未知 |  |  |  |  |  |  |

## 安全范围记录表

每个关节从 `1500` 开始，小范围逐步扩大。听到堵转、撞限位、明显抖动或发热，立即停止并回到安全位置。

| 物理舵机/关节 | 舵机 ID | 安全最小 PWM | 安全最大 PWM | 推荐零位/Home | `pos` 增大时的实际方向 | 是否可装连杆 | 备注 |
|---|---:|---:|---:|---:|---|---|---|
| 底座旋转 | 0 | 600 |2400  |1500  |逆时针  |  | 发现实际活动范围只有可以比这个值要大如果是360度的话现在活动大约为300度左右 |
| 大臂/肩关节 |1  | 700 |  2300|  1900| 向后倒 |  | 1900是大概向后弯45度感觉比较合理 并且900感觉就可以夹道地面的东西了，2300几乎与地面平行但是600到2400是可获动极限了不用再加了，1500是巡线打直|
| 小臂/肘关节 | 2 |600  | 2400|  2150| 向前弯 | 2150是低头巡线较合理垂直位 |  |
| 腕部俯仰 | 3 | 700 |2300  | 850 |仰  |  850是低头巡线垂直向下正好|  |
| 夹爪旋转 |4  |850  |2150  |1500  |  朝夹爪朝向看顺时针|这个范围是夹爪正好转90度感觉这个关节没必要全旋转  |  |
| 夹爪开合 | 5 | 1200 |  1800| 1500 |  夹紧| 1700足以夹住物块                                                           |  |
| 备用/未知 |  |  |  |  |  |  |  |
| 备用/未知 |  |  |  |  |  |  |  |

## 单舵机测试流程

1. 舵机空载或拆掉连杆，只保留供电和信号线。
2. 设置 `servo_test_enable = 1`，`servo_test_time = 1500`。
3. 设置 `servo_test_pos = 1500`，从 `servo_test_id = 0` 开始测试。
4. 每次把 `servo_test_send = 1`，观察是否有某个舵机动作。
5. 找到 ID 后，固定该 ID，依次测试 `1450 -> 1500 -> 1550`。
6. 确认方向后，再逐步测试 `1400~1600`、`1200~1800`、`1000~2000`，最后在确认不会撞限位时才试探 `600~2400` 内的真实安全边界。
7. 记录不会撞限位的最小值、最大值和推荐零位。
8. 所有单舵机记录完成后，再进行低速组合动作测试。

## 组合动作测试表

只在单舵机安全范围确认后使用，动作时间建议 `2000~3000 ms`。

| 测试编号 | 参与关节 | 起始 PWM | 目标 PWM | 动作时间 | 是否干涉 | 是否抖动/堵转 | 结论 |
|---:|---|---|---|---:|---|---|---|
| 1 | 底座 + 大臂 |  |  | 3000 |  |  |  |
| 2 | 大臂 + 小臂 |  |  | 3000 |  |  |  |
| 3 | 小臂 + 腕部 |  |  | 3000 |  |  |  |
| 4 | 腕部 + 夹爪 |  |  | 3000 |  |  |  |
| 5 | 全部回 Home |  |  | 3000 |  |  |  |
